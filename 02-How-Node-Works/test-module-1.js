class Calculator {
  divide(a, b) {
    return a / b;
  }

  multiply(a, b) {
    return a * b;
  }

  add(a, b) {
    return a + b;
  }
}

module.exports = Calculator;

// For exporting one single value;
